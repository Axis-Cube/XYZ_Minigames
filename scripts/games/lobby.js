import { EntityInventoryComponent, system, world } from "@minecraft/server";
import { MT_GAMES } from "../modules/MultiTasking/instances";
export function lobbyPlateInteraction(player, block) {
}
export function TikTakToe() {
    //world.afterEvents.itemUseOn.subscribe(ttt=>{
    //    console.log(ttt.block.getItemStack().typeId, ttt.source.name)
    //})
}
